/**
 * Domain-specific exception hierarchy. These types cross the port boundary so that
 * driving adapters can map them to protocol-level error responses without depending
 * on Java's generic exception types.
 */
package com.profitflow.domain.exception;
