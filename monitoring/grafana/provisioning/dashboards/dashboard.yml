apiVersion: 1

providers:
  - name:    ProfitFlow
    orgId:   1
    type:    file
    options:
      path: /etc/grafana/provisioning/dashboards
    disableDeletion: true
    updateIntervalSeconds: 30
